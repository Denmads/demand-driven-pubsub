syntax = "proto3";

option csharp_namespace = "ActorBackend.Actors";

import "google/protobuf/empty.proto";
import "Messages.proto";

service ClientManagerGrain {
}

service ClientGrain {
	rpc Connect (ClientConnectInfo) returns (google.protobuf.Empty);
}

service QueryResolverGrain {
	rpc ResolvePublishQuery (PublishQueryInfo) returns (google.protobuf.Empty);
	rpc ResolveSubscribeQuery (SubscribeQueryInfo) returns (google.protobuf.Empty);
}